# 医疗数据质控规则配置系统 — 前端需求规格说明书（SRS）

**版本**：v1.0  
**技术栈**：Vue 3 + Element Plus  
**文档范围**：前端页面结构、交互逻辑、接口调用、权限控制、状态管理

---

## 目录

1. 总体架构约定
2. 权限与路由设计
3. 数据源管理模块
4. 数据上传监控模块
5. 质控规则管理模块
6. 质控结果查询与分析模块
7. 数据质量报告模块
8. 系统管理模块
9. 全局公共组件规范
10. 接口规范与错误处理
11. 性能与体验要求

---

## 1. 总体架构约定

### 1.1 项目结构

```
src/
├── api/              # 所有接口封装，按模块分文件
├── assets/           # 静态资源
├── components/       # 全局公共组件
├── directives/       # 自定义指令（如 v-permission）
├── layout/           # 整体布局（侧边栏、顶栏、面包屑）
├── router/           # 路由配置（动态路由）
├── store/            # Pinia 状态管理
│   ├── auth.js       # 用户登录态、权限标识列表
│   └── dict.js       # 全局字典缓存
├── utils/            # 工具函数（request封装、格式化等）
└── views/            # 页面组件，按模块分目录
```

### 1.2 状态管理约定（Pinia）

**auth store** 在登录成功后从接口响应中读取并持久化以下内容：

| 字段 | 类型 | 说明 |
|------|------|------|
| `token` | string | JWT令牌，每次请求携带 |
| `userId` | number | 当前用户ID |
| `orgId` | number | 所属机构ID |
| `orgCode` | string | 机构代码（用于展示，不用于接口过滤，过滤由后端处理） |
| `orgName` | string | 机构名称 |
| `roleId` | number | 角色ID |
| `dataScope` | number | 数据权限范围：1-全部，2-本区县，3-本机构 |
| `permissions` | string[] | 权限标识列表，如 `['rule:add','rule:delete']` |

> **权限同步约定**：登录接口 `/api/user/login` 响应的 `data` 中必须包含 `permissions` 数组（后端从 `sys_role_permission` + `sys_permission` 联查生成）。前端据此列表动态渲染菜单和按钮，无需额外请求权限接口。

### 1.3 HTTP请求封装（utils/request.js）

- 基于 `axios`，统一设置 `Authorization: Bearer {token}` 请求头
- 响应拦截器：`code !== 200` 时统一弹出 `ElMessage.error(message)`
- HTTP 401 时清除 auth store 并跳转登录页
- 请求超时默认 30 秒，文件导出请求单独配置 120 秒

---

## 2. 权限与路由设计

### 2.1 路由结构

路由分两类：

**公开路由**（无需登录）：
- `/login` — 登录页

**受保护路由**（需要登录，动态注册）：
- `/dashboard` — 首页概览
- `/datasource` — 数据源管理
- `/upload-monitor` — 数据上传监控
- `/rule` — 质控规则管理
  - `/rule/category` — 规则分类
  - `/rule/list` — 规则列表
  - `/rule/template` — 规则模板
- `/result` — 质控结果
  - `/result/overview` — 全局统计
  - `/result/by-rule` — 按规则统计
  - `/result/detail` — 问题明细
- `/report` — 数据质量报告
- `/system` — 系统管理
  - `/system/org` — 机构管理
  - `/system/user` — 用户管理
  - `/system/role` — 角色管理

### 2.2 路由守卫

```
全局前置守卫逻辑：
1. 有 token → 已登录 → 放行（若 permissions 未加载则先调接口刷新）
2. 无 token → 跳转 /login
3. 访问无权限菜单 → 跳转 /403 提示页
```

### 2.3 权限控制实现

**自定义指令 `v-permission`**：

```vue
<!-- 有权限显示，无权限DOM不渲染 -->
<el-button v-permission="'rule:add'">新增规则</el-button>
```

**工具函数 `hasPermission(key)`**：用于 JS 逻辑判断（如动态列渲染）。

**菜单动态渲染**：侧边栏菜单项根据 `permissions` 列表过滤，无权限的菜单项不展示。

### 2.4 权限标识清单

| 权限标识 | 说明 |
|----------|------|
| `datasource:view` | 查看数据源 |
| `datasource:add` | 新增数据源 |
| `datasource:edit` | 编辑数据源 |
| `datasource:delete` | 删除数据源 |
| `datasource:sync` | 触发同步 |
| `dataset:view` | 查看数据集 |
| `dataset:edit` | 编辑数据集 |
| `dataset:sync` | 同步字段 |
| `rule:view` | 查看规则 |
| `rule:add` | 新增规则 |
| `rule:edit` | 编辑规则 |
| `rule:delete` | 删除规则 |
| `rule:test` | 测试规则 |
| `rule:export` | 导出规则 |
| `rule:import` | 导入规则 |
| `rule:batch-enable` | 批量启用/禁用 |
| `template:apply` | 应用模板 |
| `result:view` | 查看质控结果 |
| `result:export` | 导出结果 |
| `result:fix` | 标记整改 |
| `report:view` | 查看报告 |
| `report:export` | 导出报告 |
| `system:org` | 机构管理 |
| `system:user` | 用户管理 |
| `system:role` | 角色管理 |
| `upload:view` | 查看上传监控 |
| `upload:export` | 导出上传数据 |

---

## 3. 数据源管理模块

**路由**：`/datasource`  
**权限**：`datasource:view`

### 3.1 页面布局

页面分为上下两个卡片区域：
- **业务中心库** 卡片（source_type = business）
- **值域代码库** 卡片（source_type = code）

每个卡片显示当前配置的数据源信息，状态标签（启用/禁用），以及操作按钮组。

### 3.2 数据源列表展示

每个数据源卡片包含：
- 数据源名称、数据库类型（Tag标签）、IP:端口/数据库名
- 连接状态标签：绿色"连通"/ 红色"断开"/ 灰色"未验证"
- 同步频率（值域代码库专属）、上次同步时间
- 操作按钮：编辑（`datasource:edit`）、删除（`datasource:delete`）、测试连接（`datasource:view`）、同步（`datasource:sync`，仅代码库）

### 3.3 新增/编辑数据源弹窗

触发：点击"新增数据源"按钮（`datasource:add`）或"编辑"按钮。

**表单字段**：

| 字段 | 组件 | 校验规则 |
|------|------|----------|
| 数据源名称 | Input | 必填，最长100字符 |
| 数据源类型 | Select（business/code） | 必填，新增时可选，编辑时只读 |
| 数据库类型 | Select（MySQL/PostgreSQL/Oracle/DM） | 必填 |
| IP地址 | Input | 必填，IP格式校验 |
| 端口 | InputNumber | 必填，1-65535 |
| 数据库名 | Input | 必填 |
| 用户名 | Input | 必填 |
| 密码 | Password Input | 新增必填；编辑时为空表示不修改 |
| 同步频率 | Select（daily/weekly/monthly） | 仅代码库显示，必填 |

**弹窗底部操作**：
- 测试连接：调用 `POST /api/datasource/{id}/test`（已保存）或在保存前先验证连接；成功显示绿色提示，失败显示红色错误信息
- 取消 / 保存：保存前必须测试连接通过

**交互约束**：
- 密码字段编辑时显示"●●●●●●（已配置，不填则不修改）"占位提示
- 保存成功后若为值域代码库，弹出确认框提示"是否立即触发代码库初始同步？"

### 3.4 代码库同步功能

点击"立即同步"按钮（`datasource:sync`）：
1. 弹出确认对话框："将立即同步值域代码库，可能耗时数分钟，是否继续？"
2. 确认后调用 `POST /api/datasource/{id}/sync`
3. 按钮变为 loading 状态，显示"同步中..."
4. 轮询同步状态（每3秒）或 WebSocket 推送
5. 完成后刷新"上次同步时间"，显示同步结果（新增/更新/删除代码集数量）

### 3.5 数据集配置子页面

路由：`/datasource/dataset`（可从数据源页面导航进入）

**数据集列表**（`dataset:view`）：
- 表格列：数据集编码、数据集名称、机构代码字段、业务主键字段、是否必选、记录数、状态、操作
- 操作：编辑、同步字段（`dataset:sync`）、启用/禁用

**同步字段**：点击"同步字段"后，系统从业务中心库读取该表的最新字段结构，对比现有 `sys_data_element` 记录：
- 新增字段：高亮绿色展示
- 删除字段：高亮红色展示并标记删除（软删除）
- 修改字段：高亮黄色展示

用户确认后提交更新。

**编辑数据集弹窗**：

| 字段 | 说明 |
|------|------|
| 机构代码字段名 | 下拉选择当前数据集的字段（从已同步字段中选），必填 |
| 业务主键字段名 | 支持逗号分隔的联合主键，如 `cf_id,mx_xh` |
| 是否必选 | Switch |
| 排序号 | InputNumber |

---

## 4. 数据上传监控模块

**路由**：`/upload-monitor`  
**权限**：`upload:view`

### 4.1 筛选区

水平排列的筛选条件（ElForm inline模式）：

| 控件 | 说明 |
|------|------|
| 业务时间（月份） | ElDatePicker，type=month，默认当前月，必选 |
| 区县 | Select，选项来自接口（区域管理员以上可见），机构用户不显示此项 |
| 信息系统类型 | Select，选项来自数据字典 |
| 机构名称 | Select，选项根据区县联动过滤，机构用户固定为本机构 |

操作按钮：搜索、重置、导出（`upload:export`）、刷新

### 4.2 数据表格

**表格设计要求**：
- 列固定：表名（左固定）、表描述（左固定）、月累计上传量（右固定）
- 动态列：1日~31日（根据所选月份动态生成，不足31天的月份不显示对应列）
- 最后一行为合计行，背景色区分（浅灰）
- 单元格数据为0时显示"-"，非0显示数字格式化（千分位）
- 支持列宽拖拽调整
- 支持按任意列点击表头排序

**数据为空状态**：显示 Element Plus 的 `el-empty` 组件，提示"暂无上传数据，请检查筛选条件"

### 4.3 导出

调用 `GET /api/upload/daily?...&export=true`，后端直接返回 Excel 文件流，前端触发浏览器下载，文件名格式：`数据上传监控_YYYY年MM月_导出时间.xlsx`

---

## 5. 质控规则管理模块

### 5.1 规则分类管理页

**路由**：`/rule/category`  
**权限**：`rule:view`（超级管理员专属菜单）

表格列：分类名称、分类描述、执行优先级、排序号、操作（编辑、删除）  
支持拖拽调整排序号（`vuedraggable`）

### 5.2 规则列表页

**路由**：`/rule/list`  
**权限**：`rule:view`

#### 5.2.1 筛选区

| 控件 | 说明 |
|------|------|
| 规则名称 | Input，模糊搜索 |
| 规则分类 | Select，选项来自规则分类接口 |
| 规则级别 | Select（严重/警告/提示） |
| 执行频率 | Select（每日/每周/每月/手动） |
| 启用状态 | Select（启用/禁用） |

操作按钮：查询、重置、新增规则（`rule:add`）、批量启用（`rule:batch-enable`）、批量禁用（`rule:batch-enable`）、批量删除（`rule:delete`）、导入（`rule:import`）、导出（`rule:export`）

#### 5.2.2 规则列表表格

| 列 | 说明 |
|----|------|
| 选择框 | 批量操作用 |
| 规则名称 | 点击可展开规则描述 |
| 规则分类 | Tag显示 |
| 规则级别 | Tag：严重=红色，警告=橙色，提示=蓝色 |
| 执行频率 | 文字 |
| 适用范围 | 全机构/指定机构 |
| 启用状态 | Switch（`rule:edit`权限才可操作） |
| 操作 | 编辑、复制、测试、删除 |

分页：每页20条，支持切换每页数量（10/20/50/100）

#### 5.2.3 新增/编辑规则 — 步骤式弹窗

采用 `el-steps` 三步引导：

**第一步：基本信息**

| 字段 | 组件 | 说明 |
|------|------|------|
| 规则名称 | Input | 必填，最长200字符 |
| 规则描述 | Textarea | 选填 |
| 规则分类 | Select | 必填，选择10大类之一 |
| 规则级别 | RadioGroup | 严重/警告/提示，必填 |
| 执行频率 | Select | 必填 |
| 适用范围 | RadioGroup | 全机构/指定机构 |
| 适用机构 | Select（多选） | 仅"指定机构"时显示，机构用户只能看到本机构 |

**第二步：规则参数配置**

根据第一步选择的**规则分类**，动态渲染不同的配置表单：

| 规则类型 | 动态表单内容 |
|----------|-------------|
| 非空检查 | 选择数据集 → 选择数据元（单选） |
| 值域代码检查 | 选择数据集 → 选择数据元 → 选择值域代码库 → 选择代码集 |
| 字符串长度检查 | 选择数据集 → 选择数据元 → 最小长度（InputNumber） → 最大长度（InputNumber） |
| 数值范围检查 | 选择数据集 → 选择数据元 → 最小值（InputNumber）→ 最大值 |
| 日期格式检查 | 选择数据集 → 选择数据元 → 日期格式下拉（yyyy-MM-dd等常用格式） |
| 比较两个日期 | 选择数据集 → 选择当前日期字段 → 选择比较运算符 → 选择关联日期字段 |
| 日期区间包含 | 选择数据集 → 选择当前日期字段 → 选择开始日期字段 → 选择结束日期字段 |
| 单字段唯一检查 | 选择数据集 → 选择数据元 |
| 多字段联合唯一 | 选择数据集 → 多选数据元（可排序，至少2个） |
| 外键引用检查 | 选择主数据集+字段 → 选择被引用数据集+字段 |
| 同字段跨表一致性 | 选择源表+字段 → 选择目标表+字段 → 选择关联字段 |
| 代码与名称一致性 | 选择数据集 → 选择代码字段 → 选择名称字段 → 选择值域代码集 |
| 及时性检查 | 选择数据集 → 允许延迟天数 |
| 数据量波动检查 | 选择数据集 → 波动阈值（%） |
| 条件逻辑检查 | 选择数据集 → condition_expr文本框 → result_expr文本框（含语法提示） |

**条件逻辑检查特殊交互**：
- `condition_expr` 和 `result_expr` 使用代码编辑器风格的 Textarea（等宽字体）
- 输入框下方展示示例：`t.zdmc LIKE '%肺炎%'`
- 保存前调用后端语法校验接口，返回校验结果（通过/拒绝原因）显示在输入框下方

**第三步：SQL预览与测试**

- 只读代码块展示系统生成的质控SQL（语法高亮，使用 `highlight.js`）
- 提供"切换为自定义SQL"开关：开启后 SQL 变为可编辑的代码编辑器
- **测试按钮**（`rule:test`）：
  - 调用 `POST /api/rule/{id}/test`
  - 展示"执行中"进度条（最多等待30秒）
  - 返回结果以表格形式展示（最多100条问题数据）
  - 结果存入 Redis 临时缓存（后端处理），**不写入正式结果表**
  - 弹窗底部显示"测试结果仅供预览，不计入正式统计"提示文字

弹窗操作按钮：上一步、下一步、保存（最后一步）、取消

#### 5.2.4 规则批量导入

- 支持下载导入模板（Excel格式，含字段说明）
- 上传 `.json` 或 `.xlsx` 格式文件
- 上传后展示导入预检结果（成功N条，失败N条，附失败原因列表）
- 用户确认后提交正式导入

### 5.3 规则模板管理页

**路由**：`/rule/template`  
**权限**：`rule:view`

#### 5.3.1 模板列表

Tab切换：系统预置模板 / 用户自定义模板

表格列：模板名称、描述、包含规则数、创建人（自定义模板）、创建时间、操作

操作：查看明细、应用模板（`template:apply`）、删除（仅自定义模板，`rule:delete`）

#### 5.3.2 应用模板

点击"应用模板"：
1. 弹出数据集选择对话框，选择要应用到的目标数据集
2. 展示模板明细列表（规则名称、规则级别、涉及字段类型）
3. 用户可取消勾选不需要的规则项
4. 确认后调用 `POST /api/template/{id}/apply`，后端批量生成 `qc_rule` 和 `qc_rule_param` 记录
5. 应用结果展示：成功生成N条规则，跳转到规则列表页

#### 5.3.3 保存为模板

在规则列表页选中多条规则后，操作菜单中提供"保存为模板"选项，输入模板名称和描述后保存。

---

## 6. 质控结果查询与分析模块

### 6.1 全局统计概览页

**路由**：`/result/overview`  
**权限**：`result:view`

#### 6.1.1 指标卡片区（顶部横排）

5个卡片，各含主指标值和环比变化箭头：
- 总校验数据量
- 问题总数
- 问题占比（%）
- 整改率（%）
- 本月新增问题数（环比）

指标卡数据调用：`GET /api/result/summary?type=overview`

#### 6.1.2 图表区

三图并排（各占1/3宽度）：
- **近6个月问题占比趋势折线图**（ECharts line chart）
- **按规则分类问题数占比饼图**（ECharts pie chart，点击扇区可下钻）
- **按数据集问题数排名柱状图**（ECharts bar chart，横向，Top10）

所有图表支持响应式缩放，tooltip显示详细数值。

#### 6.1.3 筛选联动

顶部提供机构筛选（`dataScope=1` 的用户可选，其他用户固定为本机构）和月份筛选，切换后所有卡片和图表同步刷新。

### 6.2 按规则统计页

**路由**：`/result/by-rule`  
**权限**：`result:view`

#### 6.2.1 筛选区

数据集、规则名称（级联选择）、日期范围（DateRangePicker）、规则级别（多选）

#### 6.2.2 统计表格

| 列 | 说明 |
|----|------|
| 规则分类 | 合并同类单元格展示 |
| 规则名称 | 文字 |
| 规则级别 | Tag |
| 校验总数 | 数字，千分位格式 |
| 问题数 | 蓝色可点击链接，点击跳转问题明细页 |
| 问题数占比 | 进度条样式 + 百分比文字 |
| 整改数 | 数字 |
| 整改率 | 进度条 |

导出按钮（`result:export`）：导出当前筛选数据为 Excel

#### 6.2.3 下钻跳转

点击"问题数"链接：
- 携带 `ruleId`、`datasetId`、`statMonth` 参数跳转到 `/result/detail`
- 目标页自动填充筛选条件并查询

### 6.3 问题明细页

**路由**：`/result/detail`  
**权限**：`result:view`

#### 6.3.1 筛选区

数据集、规则名称、日期范围、整改状态（未整改/整改中/已整改/无需整改）

#### 6.3.2 明细表格

| 列 | 说明 |
|----|------|
| 规则分类 | 文字 |
| 规则名称 | 文字 |
| 数据集名称 | 文字 |
| 字段名称 | 文字 |
| 问题字段值 | 文字，超长截断，鼠标悬停 tooltip 展示完整值 |
| 问题描述 | 文字 |
| 业务数据ID | 可点击，点击弹窗展示该条业务数据的完整字段（调用后端接口动态查询） |
| 创建时间 | 日期时间格式 |
| 整改状态 | Tag（颜色区分） |
| 整改时间 | 日期格式，未整改时为空 |
| 操作 | 标记整改（`result:fix`） |

#### 6.3.3 标记整改

点击"标记整改"弹出对话框：
- 整改状态：Select（整改中/已整改/无需整改）
- 整改说明：Textarea（选填）
- 提交后调用 `PUT /api/result/detail/{id}/fix`
- 系统后台触发自动复检，复检结果通过消息通知（顶栏铃铛图标）告知用户

批量标记整改：列表页选中多条后点击"批量整改"按钮，批量提交。

#### 6.3.4 业务数据详情弹窗

点击"业务数据ID"：
- 弹窗标题：`{数据集名称} - 数据详情`
- 以键值对列表展示该条业务记录所有字段（字段名→字段值）
- 问题字段高亮红色背景标注

---

## 7. 数据质量报告模块

**路由**：`/report`  
**权限**：`report:view`

### 7.1 报告选择区

顶部横排：机构选择（`dataScope <= 2` 时可选，默认当前机构）+ 报告月份选择（月份选择器，仅显示已生成报告的月份，最多12个月）

### 7.2 报告展示

报告以类文档布局展示，从上至下：

**报告头部**：
- 报告标题（大字体）、制表日期、质控时间范围、报告生成时间

**核心指标卡片区**（横排6卡片）：
- 数据质量评分（圆形进度条样式，满分100分）
- 区域排名（如"第3名/共8家"）
- 总质控数据量
- 问题总数
- 问题占比（%）
- 环比变化（箭头+百分比）

**问题分析表格区**（两个并排表格）：
- 按规则类型统计表
- 按数据集统计表

**问题TOP10**：
- 问题规则 Top10（横向柱状图）
- 问题数据集 Top10（横向柱状图）

**综合分析**：
- 纯文本段落展示 `analysis` 字段内容（后端生成）

**操作按钮**：
- 导出PDF（`report:export`）：调用 `GET /api/report/monthly/{id}/export`，后端生成PDF并下载
- 下载问题明细Excel：下载本月所有问题数据明细

### 7.3 历史报告导航

左侧或顶部 Tab 展示最近12个月报告列表，点击切换展示内容。

---

## 8. 系统管理模块

### 8.1 机构管理

**路由**：`/system/org`  
**权限**：`system:org`（区域管理员及以上可见）

列表表格列：机构名称、机构代码、所属区县、机构类型、联系人、联系电话、状态、操作（编辑、启用/禁用）

新增/编辑弹窗字段：机构名称（必填）、机构代码（必填，唯一，格式校验）、所属区县（必填，级联选择）、机构类型、联系人、联系电话

> **前端提示**：编辑弹窗内"机构代码"字段旁展示帮助提示："机构代码必须与业务数据中的医疗机构代码字段值完全一致，否则将导致数据过滤错误。"

### 8.2 用户管理

**路由**：`/system/user`  
**权限**：`system:user`

列表表格列：用户名、真实姓名、所属机构、角色、联系电话、状态、创建时间、操作（编辑、重置密码、启用/禁用）

新增/编辑弹窗字段：用户名（必填，唯一）、密码（新增必填）、真实姓名（必填）、所属机构（Select）、角色（Select）、联系电话

重置密码：弹窗输入新密码（两次确认），调用单独的重置接口。

### 8.3 角色管理

**路由**：`/system/role`  
**权限**：`system:role`

列表展示角色名称、描述、数据权限范围、操作（编辑权限、删除）

**权限分配**：点击"编辑权限"，弹出权限树（`el-tree`），按模块分组展示所有权限标识，支持全选/反选，保存后更新 `sys_role_permission` 表。

---

## 9. 全局公共组件规范

### 9.1 页面容器组件 `<PageContainer>`

统一的页面外层容器，包含：面包屑导航、页面标题、操作按钮插槽（右对齐）、内容区插槽

### 9.2 搜索表单组件 `<SearchForm>`

统一的查询表单，包含：筛选字段插槽、查询/重置按钮（固定在右侧）、可展开/收起（超过4个字段时）

### 9.3 数据表格组件 `<DataTable>`

基于 `el-table` 封装：内置分页、loading状态、空数据状态、列宽记忆（localStorage）

### 9.4 状态标签组件 `<StatusTag>`

统一展示状态的 Tag 组件，根据 `type` prop 自动映射颜色：
- `enabled` → 绿色"启用"
- `disabled` → 灰色"禁用"
- `error` → 红色"异常"
- `warning` → 橙色"警告"

### 9.5 确认删除指令 `v-confirm-delete`

点击删除按钮时统一弹出 `ElMessageBox.confirm`，确认后才执行删除回调。

### 9.6 导出 Loading 遮罩

文件导出时（接口响应前）展示全局 loading 遮罩，提示"正在生成文件，请稍候..."，避免用户重复点击。

---

## 10. 接口规范与错误处理

### 10.1 统一响应格式处理

前端 axios 拦截器统一解包：
```javascript
// 成功时直接返回 data 字段
response.data.code === 200 → resolve(response.data.data)

// 失败时
response.data.code !== 200 → ElMessage.error(response.data.message)
```

### 10.2 分页参数约定

所有列表接口请求参数：`page`（从1开始）、`size`（默认20）

所有列表接口响应：`{ records: [], total: 0, page: 1, size: 20 }`

前端统一使用 `<DataTable>` 组件处理分页，无需每个页面重复实现。

### 10.3 错误码处理

| HTTP状态码 / code | 前端处理 |
|-------------------|---------|
| 401 | 清除 token，跳转 /login，提示"登录已过期" |
| 403 | 跳转 /403 页面，提示"无权限访问" |
| 500 | ElMessage.error 展示后端错误信息 |
| 超时 | ElMessage.error "请求超时，请重试" |

### 10.4 文件下载处理

导出类接口响应为文件流（`Content-Type: application/octet-stream`），前端统一处理：
```javascript
const blob = new Blob([response.data])
const url = URL.createObjectURL(blob)
// 从响应头 Content-Disposition 解析文件名
const fileName = decodeURIComponent(response.headers['content-disposition'].split('filename=')[1])
// 触发下载
```

---

## 11. 性能与体验要求

### 11.1 首屏加载

- 路由懒加载：所有页面组件使用 `() => import()` 动态导入
- 公共库（Vue、Element Plus、ECharts）通过 CDN 引入，不打入主包
- 首屏白屏时间目标 < 2秒（正常网络环境）

### 11.2 表格性能

- 超过1000行的表格使用虚拟滚动（`el-table-v2`）
- 数据上传监控的行列转置表格，单次渲染数据量不超过 40行×31列，无虚拟滚动需求

### 11.3 图表性能

- ECharts 图表使用 `renderer: 'canvas'`
- 数据量超过500点时，折线图开启 `sampling: 'average'` 降采样

### 11.4 操作反馈

- 所有异步操作（保存、删除、导出等）必须有 loading 状态提示
- 操作成功后展示 `ElMessage.success`，持续2秒自动消失
- 删除操作必须有二次确认弹窗

### 11.5 浏览器兼容性

支持 Chrome 90+、Edge 90+、Firefox 88+，不需要支持 IE。

---

*文档版本：v1.0 | 最后更新：2026-05*
